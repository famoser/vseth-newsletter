{% extends "single_content_base.html.twig" %}

{% form_theme form _self %}

{% import "shared/forms.html.twig" as forms %}

{% block single_content %}
    {{ form_start(form) }}
    <div class="row">
        <div class="col-md-6">
            <div class="form-group">
                {{ form_row(form.organisation) }}
            </div>
        </div>
    </div>
    <hr/>
    {{ forms.entry_base(form) }}
    {{ form_end(form) }}
{% endblock %}
